package StructuredStreaming.exercises

object StructuredStreamingOperationsExercise {
	/**
	  * Exercise:
	  *
	  * Retrieve tweets from the Kafka "tweets" topic and count how many tweets were created per minute,
	  * along with the maximum tweet length. Use the "createdAt" tweet column to group the tweets, rather
	  * than a window function.
	  */
	def main(args: Array[String]): Unit = {
		// TODO: add your solution here
	}
}
